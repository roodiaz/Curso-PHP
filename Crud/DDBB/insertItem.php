<?php

require("connection.php");

$conexion_db = getConnection();

function inserItem($cod, $prec, $talle, $tipo, $color, $tela)
{

    global $conexion_db;

    try {
    } catch (Exception $ex) {
    }
}
